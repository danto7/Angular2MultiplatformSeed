doctype html
html
  head
    title Angular2 Quickstart
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='styles.css')
    each item in vendorFiles
      script(src=item)
      
    script.
      System.config({
        packages: {
          app: {
            format: 'register',
            defaultExtension: 'js'
          }
        }
      });
      System.import('app/main').then(null, console.error.bind(window.console));
    
  body
    my-app Loading ...
